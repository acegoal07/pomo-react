import { StyleSheet } from 'react-native';

export const appColours = StyleSheet.create({
  backgroundColour: {
    backgroundColor: '#c42217',
  },
  foregroundColour: {
    color: '#eed847',
  },
});
